{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "build-go",
        "type": "shell",
        "command": "cd ${workspaceFolder}/local-app && go build -gcflags=\"all=-N -l\" -o __debug_bin",
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }